#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"Enter the two numbers: ";
    cin>>a>>b;
    string result = (a==b)?"Equal":"Not equal";
    cout<<"The numbers are: "<<result<<endl;
    return 0;
}
